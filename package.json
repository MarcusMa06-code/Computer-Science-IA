{
  "name": "online_register_system",
  "version": "2.1.0",
  "scripts": {
    "build": "tailwindcss -o src/dist/output.css --minify",
    "start": "serve -s src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "serve": "^13.0.2"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.3"
  }
}
